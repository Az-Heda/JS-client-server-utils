const all = require('./dist/server');

module.exports = all;

// console.clear();
// const { WebServer, MySurreal } = all;
// const server = new WebServer();
// const sdb = new MySurreal({ username: 'root', password: 'root', namespace: 'test', database: 'test'});
